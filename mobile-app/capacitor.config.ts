import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.onlineshop.mobile',
  appName: 'Online Shop Mobile',
  webDir: 'dist'
};

export default config;
